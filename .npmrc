# Allow build scripts for native modules like bcrypt
enable-pre-post-scripts=true

# Suppress peer dependency warnings for React 19 (works fine with @testing-library/react 14)
# These warnings appear because @testing-library/react hasn't updated their peer deps yet
# but React 19 is compatible with the library
strict-peer-dependencies=false




